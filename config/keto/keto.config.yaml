version: v0.11.1

dsn: memory

namespaces:
  - name: system
    id: 0
  - name: users
    id: 1
  - name: admin
    id: 2
  - name: nova
    id: 3
  - name: ranks
    id: 4

serve:
  read:
    port: 4466
    # CORS section removed - Oathkeeper handles all CORS
    # Keto is only accessible through Oathkeeper (Zero Trust architecture)
  write:
    port: 4467
    # CORS section removed - Oathkeeper handles all CORS
    # Keto is only accessible through Oathkeeper (Zero Trust architecture)

log:
  level: debug
  format: json
