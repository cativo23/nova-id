# Environment variables
.env
.env.local
.env.*.local
.env.production  # Production secrets - NEVER commit

# Dependencies
node_modules/

# Build outputs
dist/
build/

# Logs (runtime directories only — source under api/src/ is never ignored)
*.log
logs/
*.log.*
!api/src/**

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp
.cache/

# Database dumps and backups
*.sql.gz
*.dump
*.sql.backup
postgres_data/

# Secrets
secrets/
*.pem
*.key
*.crt
*.env.production

# Oathkeeper id_token signing key (generate locally; never commit)
config/oathkeeper/id_token.jwks.json

# Local planning scratch (superpowers plans + generated specs) — never commit
docs/superpowers/

# Local-only compose override (dev routing: --dev, oathkeeper :80, frontend /api) — never commit
docker-compose.override.yml
